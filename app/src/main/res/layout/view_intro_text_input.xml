<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        style="@style/IntroSlide">

        <TextView
            android:id="@+id/title"
            style="@style/IntroTitle"
            tools:text="@string/name" />

        <LinearLayout
            style="@style/IntroImageLayout"
            android:layout_weight="3">

            <ImageView
                android:id="@+id/image"
                style="@style/IntroImage"
                tools:ignore="ContentDescription"
                tools:src="@drawable/icons8_user_480" />
        </LinearLayout>

        <LinearLayout
            style="@style/IntroDescLayout"
            android:layout_weight="3">

            <TextView
                android:id="@+id/description"
                style="@style/IntroDesc"
                tools:text="@string/intro_name_description" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3"
            android:baselineAligned="false"
            android:gravity="center"
            android:orientation="horizontal"
            android:weightSum="1">

            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/text_input_layout"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_weight="0.9"
                app:passwordToggleEnabled="true"
                app:passwordToggleTint="@color/colorPrimary">

                <com.sapp.yupi.ui.appintro.ExtendedEditText
                    android:id="@+id/text_input"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/roboto"
                    android:gravity="center"
                    android:textColor="@color/secondaryTextColor"
                    android:textSize="18sp"
                    android:inputType="textPassword"
                    tools:hint="@string/name" />

            </com.google.android.material.textfield.TextInputLayout>
        </LinearLayout>

    </LinearLayout>
</layout>